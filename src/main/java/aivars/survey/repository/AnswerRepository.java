package aivars.survey.repository;

import aivars.survey.domain.Answer;
import aivars.survey.repository.BaseRepository;

import java.util.UUID;

public interface AnswerRepository extends BaseRepository<Answer, UUID> {
}
